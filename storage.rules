rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Users can upload/delete only their own photos
    match /photos/{userId}/{allPaths=**} {
      allow read;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
